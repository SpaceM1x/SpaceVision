{
  "name": "spacevision",
  "private": true,
  "scripts": {
    "start": "concurrently \"python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000 --app-dir backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
